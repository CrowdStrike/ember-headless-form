<textarea
  name={{@name}}
  id={{@fieldId}}
  aria-invalid={{if @invalid 'true'}}
  aria-errormessage={{if @invalid @errorId}}
  ...attributes
  {{on 'input' this.handleInput}}
>{{@value}}</textarea>