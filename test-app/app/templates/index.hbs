<HeadlessForm @data={{this.data}} @onSubmit={{this.doSomething}}>
  {{! <form.field @name="name" as |field|>
    <field.label>Name</field.label>
    <field.input @type="text" />
  </form.field>

  <form.submitButton>Submit</form.submitButton> }}
</HeadlessForm>