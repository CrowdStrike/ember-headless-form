<input
  name={{@name}}
  type={{@type}}
  value={{@value}}
  id={{@fieldId}}
  aria-invalid={{if @invalid 'true'}}
  aria-describedby={{if @invalid @errorId}}
  ...attributes
  {{on 'input' this.handleInput}}
/>